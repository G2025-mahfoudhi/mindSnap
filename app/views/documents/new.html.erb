<% content_for :title, "Nouveau document — MindSnap" %>
<div class="container py-4" style="max-width: 700px;">
  <h1 class="h3 mb-4">Nouveau document</h1>
  <% back_path = params[:from] == "dashboard" ? dashboard_path : nil %>
  <%= render "form", document: @document, back_path: back_path %>
</div>
