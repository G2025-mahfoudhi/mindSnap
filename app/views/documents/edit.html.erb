<% content_for :title, "Modifier #{@document.title} — MindSnap" %>
<div class="container py-4" style="max-width: 700px;">
  <div class="card">
    <div class="card-header text-center">
      <h1 class="h3 mb-0"><strong>Modifier <%= @document.title %></strong></h1>
    </div>
    <div class="card-body">
      <%= render "form", document: @document %>
    </div>
  </div>
</div>
